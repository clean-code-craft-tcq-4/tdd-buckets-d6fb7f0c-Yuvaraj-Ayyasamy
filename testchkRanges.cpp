#include "chkRanges.h"

int main()
{
    chkrange();
    assert(rangeCount[0]) == 3);
    assert(rangeCount[1]) == 3);
    assert(rangeCount[2]) == 0);
    //assert(*(rangeCount+) == 2);  
    return 0;
}
